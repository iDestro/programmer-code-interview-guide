#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int unformed_sum(vector<int>& arr) {
    sort(arr.begin(), arr.end());
    int range = 0;
    for (int i = 0; i < arr.size(); i++)

}

int main() {

    return 0;
}